interface PageProps {
  params: {
    id: string;
  };
}

export default function CourseDetailsLatest({ params }: PageProps) {
  // 23 comes from the URL
 // const courseId = params.id;

  return (
    <div>
      <h1>Course Details</h1>
      
    </div>
  );
}
